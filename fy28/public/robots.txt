User-agent: *
Allow: /

# Structured data and a plain-language guide to it, for anybody — human or agent —
# who wants to check this site rather than read it.
# llms.txt is a convention rather than a standard, so it is pointed at from here,
# from a <link> in the page head, and from the sitemap, because nothing looks for it
# on its own.

Sitemap: https://lunenburgbudgetproject.org/sitemap.xml
